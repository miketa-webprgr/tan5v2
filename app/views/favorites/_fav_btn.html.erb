<%= link_to change_favorite_path(user_id: user.id, wordnote_id: wordnote.id), class: "btn", id: 'favorite-no-' + wordnote.id.to_s, method: "post", remote: true do |f| %>
  <i class="fa fa-star"></i>
<% end %>

